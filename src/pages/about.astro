---
import SiteScaffold from '@/components/layout/SiteScaffold.astro';
import AboutSection from '@/components/sections/AboutSection.astro';
import ContactSection from '@/components/sections/ContactSection.astro';
import GallerySection from '@/components/sections/GallerySection.astro';
import PageHeader from '@/components/sections/PageHeader.astro';
import { createContentProvider } from '@/lib/content';

const provider = createContentProvider();
const content = await provider.getMarketingContent();
---

<SiteScaffold
  title={`${content.brand} | Nosotros`}
  description="Conoce la visión, estilo y proceso creativo de nuestro estudio fotográfico."
  brand={content.brand}
  nav={content.nav}
  pageTone="inner"
>
  <PageHeader
    title="Nosotros"
    subtitle="Dirección creativa, producción visual y un workflow sólido para entregar fotografías listas para publicar."
  />
  <AboutSection about={content.about} />
  <GallerySection gallery={content.gallery} />
  <ContactSection contact={content.contact} nav={content.nav} />
</SiteScaffold>

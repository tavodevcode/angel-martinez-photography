---
import SiteScaffold from '@/components/layout/SiteScaffold.astro';
import ContactSection from '@/components/sections/ContactSection.astro';
import PageHeader from '@/components/sections/PageHeader.astro';
import { createContentProvider } from '@/lib/content';

const provider = createContentProvider();
const content = await provider.getMarketingContent('en');
---

<SiteScaffold
  title={`${content.brand} | Contact`}
  description="Tell us about your next photo session and we will design the right scope and timeline."
  brand={content.brand}
  nav={content.nav}
  locale="en"
  homeHref="/en/"
  ctaLabel={content.hero.ctaLabel}
  ctaHref={content.hero.ctaHref}
  pageTone="inner"
>
  <PageHeader
    eyebrow="Editorial navigation"
    title="Contact"
    subtitle="Share your session type, dates, and goals. We will propose a clear production plan."
  />
  <ContactSection contact={content.contact} nav={content.nav} />
</SiteScaffold>
